[package]
name = "siar-dtn"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
siar-domain.workspace = true
# Deliberately nothing else — next.md §34: "DTN doesn't care whether
# next hop is BLE/Bluetooth Classic/Wi-Fi Direct/Wi-Fi Aware/LAN/Iroh
# Internet." No transport crate, no `tokio`, no I/O of any kind here;
# this is the routing/storage/dedup *logic* next.md §33 describes,
# operating purely on bytes and timestamps a caller supplies — same
# "pure logic, zero I/O" shape as `siar-transport-ble`'s fragment/
# reassembly/discovery modules.
